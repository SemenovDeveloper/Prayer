import { SafeAreaView, Text } from 'react-native'
import React from 'react'
import PlusIcon from 'src/assets/icons/plus-icon'

export const ColumnsList: React.FC = () => {
  return (
    <SafeAreaView>
      <Text>Collumns List</Text>
      <PlusIcon />
    </SafeAreaView>
  )
}
